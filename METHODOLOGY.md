# 한국 로또(6/45) 분석 프로젝트 — 방법론 & 목적 (v2)

> 작성일: 2026-07-08 · 개정: 2026-07-10 (GPT 비판 검토 반영)
> 목적: 과거 한국 로또 데이터를 활용해 여러 "예측/분석 방법론"의 유효성·무효성을
> 확률·통계·백테스트로 **실증 검증**하기 위한 문서.
>
> ⚠️ 본 문서는 로또 구매를 권장하지 않습니다. 확률·통계 교육 및 실증 설계 목적입니다.

---

## 0. 귀무모형 (Null Model) — ※ "Ground Truth" 아님

한국 로또 6/45의 **공식 게임 설계**는 45개 번호 중 6개를 무작위 추첨하는 구조다.
본 프로젝트의 **기본 귀무모형(null model)** 은 다음과 같다.

- 표본공간: `Ω = { S ⊂ {1,…,45} : |S| = 6 }`, 전체 조합 수 `|Ω| = C(45,6) = 8,145,060`
- 귀무가설: `Yₜ ~ IID Uniform(Ω)` — 각 회차는 서로 독립이고, 모든 6-조합이 동일 확률 `1/8,145,060`

> **중요**: 이것은 "증명된 사실"이 아니라 **검정 대상인 가설**이다.
> 현실의 물리 추첨(추첨기·공 세트·운영 절차)이 이 귀무모형과 일치하는지는
> 데이터로 **검정·감사할 대상**이다. 따라서 문서 전반에서 IID uniform을
> "결론"이 아니라 "출발점(귀무가설)"으로 취급한다.

### 예측 불가능성은 "조건부" 명제다
> **IID uniform 모형이 참이라면**, 과거 당첨번호만으로 다음 회차 조합의 적중확률을
> 무작위 선택보다 높이는 것은 불가능하다.
>
> 수식으로: 어떤 과거 정보 `𝓗ₜ = σ(Y₁,…,Yₜ)` 에 대해서도
> `P(Y_{t+1}=S | 𝓗ₜ) = 1/8,145,060`. 과거를 보고 분포 `q(S)`로 한 장을 골라도
> `Σ q(S)·(1/8,145,060) = 1/8,145,060` 으로 확률은 불변. m장이면 정확히 `m/8,145,060`.

단, **물리적 편향·규칙 변경·장비 결함·데이터 오류** 같은 비-IID 요인이 있다면
이는 처음부터 배제할 수학적 진리가 아니라 **공정성 검정의 대상**이다.

### 회차 간 독립 ≠ 회차 내 독립
한 회차 안에서는 비복원 추출이라 번호들이 **독립이 아니다**.
- `P(i ∈ Yₜ) = 6/45 = 2/15`
- `P(i,j ∈ Yₜ) = C(43,4)/C(45,6) = 1/66`, 그런데 독립이면 `(2/15)² = 4/225`
- `1/66 < 4/225` → **음의 의존성**. 통계 검정 설계에서 반드시 반영해야 함.

---

## 1. 프로젝트 목적 (Goals)

1. **데이터 확보**: 동행복권 공개 데이터(회차·당첨번호·보너스·등수별 당첨자수/당첨금·메타데이터) 수집·검증
2. **탐색적 분석(EDA)**: 빈도, 홀짝/고저, 합계, 연속번호, 미출현 회차 시각화 (항상 IID 시뮬레이션 구간 병기)
3. **공정성 검증**: Monte Carlo null 기반 통계 검정으로 IID uniform 귀무가설 검정
4. **예측 시도 & 실증**: 여러 전략을 walk-forward 백테스트하여 **무작위 baseline과 구별되는지** 엄밀히 검정
5. **EV·커버리지 분석**: 만델/휠링/공동당첨을 "예측"이 아니라 "구매 구조의 수학"으로 재분류
6. **교육적 정리**: 각 방법론의 한계·함정을 명확히 문서화

> **성공 조건은 "좋은 예측기"가 아니라 "좋은 반증(反證) 설계"다.**
> 핵심 메시지: "맞추기 위한 프로젝트"가 아니라 **"왜 못 맞추는지 엄밀히 실증하는 프로젝트"**

---

## 2. 방법론 목록 (Methodologies)

### 2.1 빈도 기반 (Frequency Analysis) — 예측력 ❌
- 핫/콜드/미출현 기간. IID에서 `P(i∈Y_{t+1} | Xᵢ(T)=x) = 2/15` (과거와 무관).
- `Xᵢ(T) ~ Binomial(T, 2/15)`. T=1200이면 기대 160회·표준편차 ≈11.8회 → 빈도 차이는 **신호가 아니라 표본 변동**.
- 콜드/미출현은 전형적 **도박사의 오류**. r회 연속 미출현 확률 `(13/15)^r` (30회면 ≈1.37%, 희귀하지만 45개×장기간이면 관찰됨).
- **가치**: 예측❌ / 데이터 품질 점검·EDA·"극단값은 생각보다 자주 생긴다" 교육.

### 2.2 조합/패턴 기반 (Pattern Analysis) — 예측력 ❌ (조합군 설명용)
- 패턴은 **조합군 `A`의 확률 `|A|/|Ω|`** 을 설명할 뿐, 개별 조합 1등 확률은 언제나 `1/8,145,060`.
- 홀짝: 홀수 k개 확률 `C(23,k)C(22,6-k)/C(45,6)` → 3:3이 33.5%로 최다, 0:6은 0.92%. **하지만 3:3 한 장 = 6:0 한 장 (동일 확률)**.
- 합계: 평균 138, 표준편차 ≈29.95, 범위 21~255. 100~170 구간 ≈75.5%. "정규분포"보다 "유한모집단 비복원 조합합의 이산분포(종 모양)"가 정확.
- **연속번호: 하나 이상 포함 확률 ≈52.9%** — "연속은 잘 안 나온다"는 대표성 휴리스틱 오해. 절반 이상이 연속 포함!
- 구간 분할은 길이가 다르면(예: 40~45는 6개 vs 10~19는 10개) 기대빈도가 달라 단순비교 금지.
- **가치**: 예측❌ / EDA·무작위 생성기 품질점검·**공동당첨 리스크 회피**(인기조합 기피 → 확률이 아니라 분할위험 감소).

### 2.3 통계적 검정 (Statistical Testing) — 예측❌ (공정성 검증용)
- 목적: `H₀: Yₜ ~ IID Uniform(Ω)` 에 반하는 증거 탐색. **기각 실패 ≠ 공정성 증명**.
- ⚠️ **단순 카이제곱은 부정확**: 회차 내 비복원 구조로 `Cov(Xᵢ,Xⱼ) = T(1/66 − 4/225) < 0`.
  통계량을 χ²₄₄와 비교하면 틀림 — 실제 기대값은 44가 아니라 **39**.
  → **Monte Carlo null 시뮬레이션**(동일 T로 균등 6-조합 10만회↑ 생성) 또는 비복원 분산 반영, 번호별 이항검정+다중비교 보정.
- 번호쌍 `C(45,2)=990`개 → **다중비교 문제 극심**. 런검정·자기상관도 lag×번호 다중검정 보정 필수.
- **가치**: 데이터 오류 탐지·공정성 이상 징후·"유의하지 않음 ≠ 효과 없음" 교육.

### 2.4 머신러닝 / 시계열 (ML & Time Series) — 예측❌ (과적합 실증용)
- IID uniform에서 proper scoring rule 최적 예측은 uniform. 조합 log loss 최소값은 `log(8,145,060)`, 번호별 최적확률은 `2/15`.
- 복잡한 모델이 나아 보이면 대개: 과적합 / 데이터 스누핑 / random split로 미래정보 누수 / 하이퍼파라미터를 테스트셋에 맞춤.
- 마르코프·Apriori: 번호쌍 기대 동시출현 `T/66`(T=1200이면 ≈18회), 3번호↑는 급감 → **희소 데이터 과적합**.
- **가치**: 예측❌ / **negative control**(라벨 셔플 후에도 같은 성능?)·calibration·train/test·스누핑 위험 실증.
- 딥러닝(LSTM/Transformer)은 **후반부 부록**으로 강등 — 데이터 크기 대비 가치 낮음.

### 2.5 조합론 / 시스템 베팅 (스테판 만델) — 예측❌ (완전 커버리지)
- 전량 매수 `8,145,060 × 1,000원 = 81.45억 원` → 1등 **보장(커버리지)** 이지 "예측"이 아님.
- ⚠️ **"잭팟 > 81.5억이면 EV>0"은 과도한 단순화**. 실제 EV엔 아래가 모두 필요:
  공동당첨자 수, 2~5등 당첨금 구조, 세금, 판매한도·발권 가능성, 이월금, **본인 대량구매가 판매액·당첨자수에 미치는 효과**.
  `EV₁등(c) = P(c 당첨)·E[ J₁ / (1 + W_c) ]` (J₁=1등풀, W_c=같은 조합 산 타 당첨자).
- 전량 매수 시 보유 티켓: 2등 6장, 3등 228장, 4등 11,115장, 5등 182,780장. **환급률 ~50% 구조에선 이월 등 특수조건 없이는 양의 EV 어려움**. 당첨 보장 ≠ 수익 보장.
- **가치**: 기대값 계산·아비트리지 조건 교육.

### 2.6 휠링 시스템 (Wheeling) — 예측❌ (조합론적 배분)
- Full wheel: N개에서 `C(N,6)` 장. 1등 확률 `C(N,6)/C(45,6)` = **동수의 무작위 조합과 동일**. (N=10이면 210장, 0.00258%)
- "보장"은 **조건부 보장**("고른 N개 안에 실제 당첨 k개↑ 들어오면 특정 등수 보장"). N을 고르는 데 예측 신호가 없으면 보장도 조건부.
- 분산은 **항상 줄지 않는다** — 어떤 휠은 결과를 상관시켜 "맞으면 여럿 같이, 틀리면 같이 틀림"으로 재배분.
- **가치**: 조합설계 교육·coverage vs probability 차이·조건부 보장의 함정.

---

## 3. 방법론 요약 표 (수정판)

| # | 방법론 | 예측력 | 더 정확한 분류 |
|---|--------|:------:|----------------|
| 2.1 | 빈도 (핫/콜드/미출현) | ❌ | EDA·데이터 품질·도박사 오류 교육 |
| 2.2 | 패턴 (홀짝/합계/연속) | ❌ | 조합군 분석·공동당첨 리스크 회피 |
| 2.3 | 통계 검정 (카이제곱 등) | ❌ | 공정성 검증 (Monte Carlo null 필수) |
| 2.4 | 머신러닝 / 시계열 | ❌ | 과적합·negative control 실증 |
| 2.5 | 조합론 (만델) | ❌ | 예측 아님 = **완전 커버리지 / EV 관리** |
| 2.6 | 휠링 | ❌ | 커버리지·조건부 하위당첨 배분 |

> 이전 v1에서 만델을 "✅"로 표기한 것은 오류였음 — 커버리지는 예측력이 아님.

---

## 4. 핵심 통계적 함정 (반드시 회피)

| 함정 | 내용 | 회피법 |
|------|------|--------|
| 도박사의 오류 | "오래 안 나왔으니 나올 때" | 조건부확률 `2/15` 병기, 미출현 구간별 백테스트 |
| 핫핸드 오류 | "최근 자주 나오니 추세" | 무작위 baseline 대비, 윈도우 다중비교 보정 |
| 과적합 | 수많은 패턴·모델 중 우연히 맞음 | 테스트셋 1회만, negative control(라벨 셔플) |
| 다중비교 | 45번호·990쌍·여러 lag·모델 | Bonferroni/Holm/BH-FDR, Westfall-Young maxT |
| 데이터 스누핑 | 데이터 보고 규칙 만듦 | 사전등록, 탐색/검증 데이터 분리 |
| 생존 편향 | 성공 전략만 보고 | 시도한 전 전략 목록·분포 보고 |
| 룩어헤드 | 전체 빈도를 과거 백테스트에 사용 | 시점 t엔 1..t-1회차만 사용 |
| 검정력 부족 | "유의X → 완전 공정" 결론 | power analysis, 검출 가능 효과크기 병기 |
| 비정상성 | 전 기간을 하나로 취급 | 장비·규칙 변경 시점 기록, 구간별 검정 |
| 잘못된 지표 | 1등 적중만 평가 | 번호적중수·등수별·ROI·Brier·log loss 병용 |

---

## 5. 백테스트 설계 원칙

- **시간순 분리**: random split 금지. Train 60% / Validation 20% / Test 20%, 또는 expanding window (시점 t엔 1..t-1만).
- **Baseline**: (A) 완전 균등 무작위 (B) 동일 패턴 조건부 무작위 (C) 고정번호 (D) 라벨 셔플 negative control.
- **평가지표**: 등수별 당첨수(기대 `E[Nᵣ]=Σ mₜ pᵣ`), 번호적중수 `P(M=j)=C(6,j)C(39,6-j)/C(45,6)`, 세전/세후 ROI, Brier, log loss, baseline percentile, Monte Carlo p-value.
- **유의성 검정**: Monte Carlo randomization test `p = (1 + #{S_r ≥ S_obs})/(R+1)`, paired comparison, 다중전략 max-statistic 보정.
- **결과 표현**: "우위 없음을 증명" ❌ → "사전 정의 테스트셋에서 무작위 baseline 대비 유의한 우위 미발견, effect size와 신뢰구간 병기" ⭕.

---

## 6. 권장 실행 로드맵 (우선순위)

| 순위 | 항목 | 이유 |
|:---:|------|------|
| 1 | **Null simulator** | 모든 검정·백테스트의 기준선 |
| 2 | 데이터 파이프라인/검증 | 오류 데이터면 전부 무의미 |
| 3 | EDA + 조합론 분포 | 교육 효과 최대 |
| 4 | 공정성 검정 | 핵심 실증 파트 |
| 5 | 단순 전략 백테스트 | 핫/콜드/패턴 무효성 검증 |
| 6 | ML negative control | 과적합 교육 |
| 7 | EV/공동당첨 분석 | 유일하게 합리적인 실용 파트 |
| 8 | 휠링 | 조합론 교육 |
| 9 | 딥러닝 | 비용 대비 낮음(부록) |
| 10 | 고차 Apriori/마르코프 | 예측 목적이면 시간 낭비 |

### 산출 문서
- `METHODOLOGY.md` (본 문서, 개정판)
- `BACKTEST_PROTOCOL.md` (walk-forward·baseline·지표·유의성 검정 상세)
- `STATISTICAL_TEST_PLAN.md` (사전등록: 검정 목록·다중비교·검정력)

---

## 7. 프로젝트 3대 원칙 (검토 반영)

> **첫째, IID uniform은 결론이 아니라 귀무모형이다.**
> **둘째, 패턴은 조합군의 확률을 설명할 뿐 개별 조합의 당첨확률을 높이지 않는다.**
> **셋째, 백테스트의 목적은 좋아 보이는 전략을 찾는 것이 아니라, 무작위 baseline과 구별되는지 엄격히 검정하는 것이다.**

---

## 부록: v1 → v2 주요 수정 이력

1. "Ground Truth" → **"Null Model(귀무모형)"** 재정의
2. "예측 이론상 불가능" → **"IID uniform이 참이라면"** 조건부 명제로 정정
3. 카이제곱 → **회차 내 비복원 음의 공분산 반영, Monte Carlo null 필수** (χ² 기대값 44→39)
4. 만델 요약표 **"✅ → ❌ (완전 커버리지)"**, EV는 세금·공동당첨·이월금 반영 필요로 정정
5. 휠링 "분산 감소" → **"항상 감소하지 않음, 조합론적 배분"** 으로 정정
6. 연속번호 **≈52.9%로 절반 이상 출현** 사실 추가 (오해 교정)
7. 통계적 함정 10종·백테스트 설계·실행 로드맵 신설
