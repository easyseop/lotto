# 한국 로또(6/45) 분석 프로젝트 — 방법론 & 목적

> 작성일: 2026-07-08
> 목적: 과거 한국 로또 데이터를 활용해 "번호 예측" 및 "통계적 분석"을 시도하는 여러 방법론을 정리하고,
> 각 방법론의 이론적 타당성을 검토하기 위한 문서.

---

## 0. 대전제 (Ground Truth)

한국 로또 6/45는 **매 회차가 서로 독립적인 균등 무작위(uniform random) 추첨**이다.

- 45개 숫자 중 6개를 비복원 추출 → 전체 조합 수 = `45C6 = 8,145,060`
- 각 조합의 당첨 확률은 **정확히 동일** (`1 / 8,145,060 ≈ 0.0000123%`)
- 과거 회차의 결과는 다음 회차에 **아무런 영향을 주지 않음** (독립 사건)

> ⚠️ 따라서 "과거 데이터로 다음 번호를 통계적으로 유리하게 예측"하는 것은 **이론상 불가능**하다.
> 이를 부정하는 논리는 대부분 **도박사의 오류(Gambler's Fallacy)** 또는 **과적합(Overfitting)** 에 해당한다.
>
> 본 프로젝트의 진짜 목적은 "예측 성공"이 아니라, **아래 방법론들이 왜/얼마나 유효하거나 무효한지를
> 실제 데이터로 검증(reality check)** 하는 데 있다.

---

## 1. 프로젝트 목적 (Goals)

1. **데이터 확보**: 동행복권 공개 회차 데이터를 수집·정제하여 재현 가능한 데이터셋 구축
2. **탐색적 분석(EDA)**: 번호 빈도, 홀짝/고저 비율, 합계 분포, 미출현 회차 등을 시각화
3. **공정성 검증**: 통계 검정(카이제곱 등)으로 추첨이 실제로 균등한지 확인
4. **예측 시도 & 실증**: 다양한 예측 방법론을 구현하고 백테스트하여 **"무작위 대비 우위가 없음"을 실증**
5. **교육적 정리**: 각 방법론의 한계와 함정을 명확히 문서화

> 핵심 메시지: **"맞추기 위한 프로젝트"가 아니라 "왜 못 맞추는지 증명하는 프로젝트"**

---

## 2. 방법론 목록 (Methodologies)

### 2.1 빈도 기반 (Frequency Analysis)
- **핫넘버(Hot)**: 자주 나온 번호를 선택
- **콜드넘버(Cold)**: 안 나온 번호를 선택
- **미출현 기간(Overdue)**: 마지막 등장 후 경과 회차 기반 선택
- 검토 포인트: 핫/콜드는 서로 모순되는 논리이며, 균등분포에서는 둘 다 무의미해야 함

### 2.2 조합/패턴 기반 (Pattern Analysis)
- **홀짝 비율** (예: 3:3, 4:2 분포)
- **고저 비율** (1~22 vs 23~45)
- **번호 합계 분포** (6개 합, 대략 정규분포 형태로 100~170 구간 집중)
- **연속번호** 포함 여부
- **끝자리/구간별 분포** (1~9, 10~19, …)
- 검토 포인트: 예측이 아니라 **"비현실적 조합 필터링"** 용도. 확률 자체는 불변.

### 2.3 통계적 검정 (Statistical Testing)
- **카이제곱 적합도 검정(Chi-square goodness-of-fit)**: 번호 출현빈도의 균등성 검정
- **런 검정(Runs test)**, **자기상관(Autocorrelation)**: 회차 간 패턴/편향 존재 여부
- 검토 포인트: 공정한 추첨이라면 "편향 없음"으로 나와야 정상. 예측이 아닌 **공정성 검증** 도구.

### 2.4 머신러닝 / 시계열 (ML & Time Series)
- **분류/회귀 모델**: 과거 회차 → 다음 번호 예측
- **시퀀스 모델** (LSTM/RNN/Transformer): 번호 시퀀스 학습
- **마르코프 체인**: 번호 간 전이확률 모델링
- **연관규칙(Apriori)**: 번호 동시출현 패턴
- 검토 포인트: 무작위 데이터에 대한 **과적합** 위험. 백테스트 성능과 실제 예측력의 괴리 확인 필요.

### 2.5 조합론 / 시스템 베팅 (Combinatorics — 스테판 만델 방식)
- 전체 조합 전량 매수로 확률을 100%로 만드는 전략
- 6/45 기준: 814만 조합 × 1,000원 ≈ **81.5억 원** 필요 → 잭팟이 이보다 커야 이득(EV > 0)
- 검토 포인트: "예측"이 아니라 **기대값(EV) 관리**. 현실적으로 비용 > 수익이라 성립 어려움.

### 2.6 휠링 시스템 (Wheeling)
- 선택한 N개 숫자로 조합을 효율적으로 배분해 하위 등수 당첨을 "보장"
- 검토 포인트: 확률을 높이는 게 아니라 **당첨 분산(variance)을 줄이는** 기법.

---

## 3. 방법론 요약 표

| # | 방법론 | 실제 예측력 | 주 용도 |
|---|--------|:----------:|---------|
| 2.1 | 빈도 분석 (핫/콜드/미출현) | ❌ 없음 | 탐색적 데이터 분석(EDA) |
| 2.2 | 패턴 분석 (홀짝/합계/연속) | ❌ (필터용) | 조합 필터링·시각화 |
| 2.3 | 통계 검정 (카이제곱 등) | ❌ (검증용) | 추첨 공정성 검증 |
| 2.4 | 머신러닝 / 시계열 | ❌ (과적합) | ML 한계 실증 교육 |
| 2.5 | 조합론 (만델) | ✅ (단, 비용>수익) | 기대값 계산 |
| 2.6 | 휠링 | 확률 X, 분산↓ | 당첨 분산 관리 |

---

## 4. 검증하고 싶은 핵심 질문 (Open Questions)

1. 카이제곱 검정 결과, 한국 로또 번호 분포는 통계적으로 균등한가? (귀무가설 기각 여부)
2. 핫/콜드 전략을 실제 과거 데이터로 백테스트하면 무작위 선택 대비 성과 차이가 있는가?
3. ML 모델의 백테스트 성능은 무작위 baseline과 유의미하게 다른가, 아니면 과적합인가?
4. 홀짝·합계 등 "패턴 필터"가 실제 당첨 조합의 특성과 일치하는가? (필터의 통계적 근거)
5. 위 방법론들 중 **교육적/실용적으로 구현할 가치**가 있는 것은 무엇인가?
